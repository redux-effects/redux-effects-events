{
  "name": "redux-effects-events",
  "description": "Setup event listeners for various things",
  "repository": "git://github.com/redux-effects/redux-effects-events.git",
  "version": "0.2.6",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && babel src --out-dir lib",
    "postpublish": "rm -rf lib"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babel-tape-runner": "^2.0.0",
    "tape": "^4.2.0"
  }
}
